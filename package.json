{
  "name": "@colinbendell/human-json",
  "version": "1.3.0",
  "author": "Colin Bendell <colin@bendell.com>",
  "description": "Human readable JSON stringifier with zero dependencies",
  "repository": "https://github.com/colinbendell/human-json",
  "homepage": "https://github.com/colinbendell/human-json",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "humanjson": "dist/cli.js"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "mkdir -p dist && cp src/* dist/ && chmod +x dist/cli.js && tsc",
    "test": "bun test && bun run format && bun run lint:fix && bun run typecheck",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "update-examples": "./scripts/update-examples.js",
    "prepublishOnly": "bun run build && bun run test"
  },
  "keywords": [
    "json",
    "stringify",
    "format",
    "human-readable",
    "pretty-print",
    "formatter"
  ],
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "bun-types": "^1.3.2",
    "eslint": "^9.39.1",
    "globals": "^16.5.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
